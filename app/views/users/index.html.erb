<div class="container-fluid bg-light p-4" style="margin-left: 25vw">
  <% @users.each do |user| %>
  <div class="d-flex flex-row bg-light text-dark">
  <%=user.username%><br/>
  <p class="mx-2">  <%= link_to '  See Profile',  user_path(user), class: "text-dark text-decoration-none" %></p>
  </div>
  <%end%>
</div>