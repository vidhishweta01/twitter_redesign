</head>
<body>

<div class="sidenav">
  <%if user_signed_in? %>
  <div class="float-right rounded">
<%= image_tag(current_user.avatar_thumbnail, class: "rounded-circle w-50 h-50")  %>
</div>
<br/>
 <p style="font-size:12px; color: rgb(241, 227, 227);">Followers <%=current_user.followers(current_user)%> Followings <%=current_user.following(current_user)%></p>
  <%= link_to "Account", edit_user_registration_path %>
  <%= link_to "Logout", destroy_user_session_path, method: :delete %>
  <%end%>
  <%= link_to "Users",users_path%>
  
</div>
   
</body>
</html> 